#!/usr/bin/python3
"""Prototype for matrix multiplication function using NumPy."""


import numpy as np

def lazy_matrix_mul(m_a, m_b):
    """Declaration and the required process as follows:"""
    return (np.matmul(m_a, m_b))
